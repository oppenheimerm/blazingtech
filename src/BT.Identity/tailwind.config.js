/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class',
    content: ['./**/*.{razor, html}'],
    theme: {
        extend: {},
        fontFamily: {
            sans: ["'Oswald'", "sans-serif"]
        }
    },
    plugins: [],
}

